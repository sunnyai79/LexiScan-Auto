# LexiScan Auto — Python Dependencies
# Requires Python 3.11 (conda env) — NOT compatible with Python 3.13
# Create env: conda create -n lexiscan python=3.11 -y
# Install:    pip install -r requirements.txt

# Web Framework
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9    # File upload support
aiofiles==23.2.1           # Required by FastAPI StaticFiles

# PDF & OCR
# PyMuPDF 1.24.11 has prebuilt wheels for Python 3.11 on Windows x64
# Do NOT use pymupdf==1.24.3 on Python 3.13 - it fails to compile from source
PyMuPDF==1.24.11
pytesseract==0.3.10        # Python wrapper for Tesseract OCR
Pillow==11.1.0            # Image processing for OCR

# NLP & NER
spacy==3.7.4               # Core NLP framework
en-core-web-sm @ https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.7.1/en_core_web_sm-3.7.1-py3-none-any.whl
# For production, use BERT model:
# en-core-web-trf @ https://github.com/explosion/spacy-models/releases/download/en_core_web_trf-3.7.3/en_core_web_trf-3.7.3-py3-none-any.whl
# spacy-transformers==1.3.4  # BERT/transformer support

# Data Validation
pydantic==2.7.1

# Utilities
python-dotenv==1.0.1
httpx==0.27.0              # Async HTTP client for testing
